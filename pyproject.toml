[build-system]
requires = ["maturin>=1.6,<2.0"]
build-backend = "maturin"

[project]
name = "transportations_library"
dynamic = ["version"]
description = "CrossTraffic transportations library"
authors = [
    { name="raynbowy23" },
]
readme = "README.md"
requires-python = ">=3.10,<3.14"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = []

[project.optional-dependencies]
test = [
    "pytest>=6.0",
    "pytest-cov",
]
# Only needed by the ad-hoc PDF-to-markdown extraction scripts (test_pdf.py),
# not by the library itself. Install with: pip install transportations_library[pdf]
pdf = [
    "pymupdf>=1.26.6",
    "pymupdf-layout>=1.26.6",
    "pymupdf4llm>=0.0.17",
]

[tool.maturin]
features = ["with-python"]
module-name = "transportations_library"

[project.urls]
Homepage = "https://github.com/crosstraffic/transportations_library"
